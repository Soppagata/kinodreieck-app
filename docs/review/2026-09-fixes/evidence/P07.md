# P07 · Streaming/Katalog und Tastaturdetails

Stand: 17.09.2026. Ergebnis: **GEBAUT, fokussiert lokal geprüft**, zur Integration durch den Meister.
Basis: `a2c6b60c4b4fbc59ccbaaa0745bd369eb4518583`.
Branch: `codex/review49-p07-streaming-20260917`.
Worktree: `/private/tmp/kd-review49-p07`.

Die sieben Originaltickets wurden gegen diese Basis erneut nachvollzogen. Alle Ursachen waren noch anwendbar: rohe Status-ID im Erstellen-/Gesehen-Pfad; lockerer Legacy-ID-Abgleich; fehlender finaler Generationencheck; verlorene Quellfrist; fehlende manuelle Page-Revision; einseitige Fristenbuchanbindung; reine Maus-Disclosure. Die Originaltickets und das Gesamtregister wurden nicht verändert.

## Vertrag und Umfang

- Navigation, Erstellung und Gesehen-Übernahme verwenden die aktuelle vollständige Masterzuordnung. Unbekannte historische Felder und Gesehen bleiben erhalten. Fehlende historische Typ-/Jahrevidenz wird nicht als sichere Identität akzeptiert.
- Known und Discover unterschiedlicher Generationen erzeugen einen erkennbar eingeschränkten Bestand ohne aktuelle Angebotsbehauptung. Ein weiterer zulässiger Abruf kann ihn heilen; die legitime Same-Generation-Union bleibt erhalten.
- Quellenablauf, Neu-Ablauf und ausdrückliche Refreshrevision bleiben getrennte Signale.
- Qualifizierte Seitenanker werden im bestehenden owner-/auswahlgebundenen Geräte-Fristenbuch gespeichert. Es gibt keinen neuen Serverwrite oder verpflichtenden Vollkatalogabruf.
- Neue native Disclosurebuttons enthalten keine Kindcontrols. Bestehende Kartenklicks, Suchfokus und Kindaktionen bleiben erhalten.
- Auth, Account, Storage, Finder, Wochenplan, Programm, Radar/Faktencontext, Filmwissen, Entdeckenprojektion und fremde Styles sind unverändert. Der StartTab-Propvertrag samt `onSpringeZuKino` ist unverändert. `node_modules` blieb ein read-only verwendeter Symlink; keine Dependencies oder Package-Dateien geändert.

## Ticketnachweise

### KD-REV-E06-002 · Verwaiste Mediathek-ID

`StreamingTab` bestätigt die Identität für alle drei Entscheidungen gegen denselben aktuellen Master. Eine gespeicherte tote ID blockiert weder Erstellen noch Übernahme. Die gezielte Gesehen-Änderung entfernt nur die verwaiste Verknüpfung. Automatisches Bereinigen anhand einer progressiven Teilseite bleibt ausgeschlossen; bei `master=null` werden keine neuen Zuordnungsentscheidungen ausgeführt.

- `node review49_p07_browser_test.mjs`: tatsächliche Komponenten, Formular, Mapper und `personalDataTransactionController` mit synthetischen Transaktionen. In Alles **und** Neu: anlegen, regulär löschen, wieder anlegen/übernehmen; tote Navigation verborgen; bestehender Eintrag verhindert doppelte Erstellung; Gesehen bleibt nach erneutem Löschen erhalten. Ungeladener Master mit vorhandenem Gesehen-/ID-Status erzeugt keinen Cleanup-Write. Chromium und WebKit bestanden.
- `node review49_p07_state_test.mjs`: gültige Verknüpfung außerhalb der sichtbaren Teilseite bleibt bestehen.
- `node streaming_pin_neu_test.mjs`, `node streaming_progressive_ui_test.mjs`, `node staffeln_test.mjs`: bestehende Status-, Fokus-, Seiten- und Legacyverträge bestanden.
- Grenze: lokale Mocktransaktionen und Gerätespeicher; keine echte Kontosynchronisation oder physische Geräteabnahme.

### KD-REV-E11-003 · Konfliktbewusster Legacy-Abgleich

`gleicheMediathekStatusAb` und die Navigation verwenden den vorhandenen strengen Identitätsresolver. Ein noch existentes falsches Ziel genügt nicht mehr. Bei bestätigtem Konflikt entfällt allein dessen Mediathekverknüpfung; Gesehen und unabhängige Zusatzfelder bleiben erhalten.

- `node review49_p07_state_test.mjs`: aktuelle Katalogprojektion, Statusabgleich und progressiver Mapper für Typ-, Jahres-, weitere-ID-Konflikte, doppelte/widersprüchliche Kandidaten und umgekehrte Masterreihenfolge. Vorhandene falsche Links werden verworfen; eindeutige gültige Identität bleibt positiv.
- `node review49_p07_browser_test.mjs`: gerenderter Legacy-Alles-Pfad mit noch lebendem falschem Link zeigt keine falsche Navigation und bietet eigene Übernahme; historisches Feld bleibt erhalten.
- `node staffeln_test.mjs` und `node streaming_identity_index_test.mjs`: positive Fixtures enthalten nun die tatsächlich erforderliche Typ-/Jahrevidenz; zusätzliche Gegenprobe für fehlende historische Evidenz. `node streaming_page_state_test.mjs` behält den gemockten Missing-RPC-Fallback bei.
- Grenze: keine Aussage über aktuelle reale Kollisionen oder Live-RPC-Nutzung.

### KD-REV-E05-001 · Gemischte Kataloggenerationen

Der echte App-Ladecallback prüft auch das Ergebnis des Known-Nachzugs. Bei Mismatch bleibt Discover erneut ladbar und der Zustand sichtbar eingeschränkt. `baueStreamingAnsichten` veröffentlicht aus einem gemischten Paar keine Titel-/Angebotsunion, einschließlich MotN-Overlay; ein passendes Paar bleibt unverändert nutzbar.

- `node review49_p07_generation_test.mjs`: führt den unverändert extrahierten **aktuellen** App-Callback mit echtem Assetloader, Katalog-/Streamingprojektion und Finder-Herkunft aus. Known A → Discover B → Cache A, geworfener Read ohne Cache, erfolgreicher falscher A sowie erfolgreicher B werden geprüft. Kein alter Netflix-Dienst oder Link gelangt in Finder, Badges/Legacy-Programm, Alles oder Neu. Fehlerzustand verriegelt Discover nicht; späteres B heilt. Same-Generation-Union bleibt positiv.
- `node katalog_test.mjs`: Quellenmetadaten, Projektion, MotN und bestehende Identitätsverträge bestanden.
- Grenze: Callback-/Consumerdurchstich mit Mocks, kein kompletter App-Boot im Browser und keine echte Netzstörung.

### KD-REV-E06-001 · Echte Quellablauffrist

Der RPC liefert `sourceExpiresAt` aus `meta.gueltig_bis` getrennt von `nextExpiryAt`. Normalisierung, Cachefrische, Controller, Wiederaufnahme und UI behalten diesen absoluten Termin. Eine erneute stale-200-Antwort markiert den Stand weiter veraltet und erzeugt keine Refreshschleife; `null` bleibt ohne erfundenen Ablauf.

- `node review49_p07_pg_test.mjs`: aktueller vollständiger RPC in frischem PG17 liefert endliche, bereits abgelaufene und unbeschränkte Quellfrist; erneute gleiche Projektion behält dieselbe Frist. Dieser Teil läuft nach Wiederherstellung der bytegenauen neuen Migration mit echter DB-Zeit.
- `node review49_p07_state_test.mjs`: offener Tab, Ablauf-Timer, stale-200, Wiederaufnahme, bereits abgelaufener Start ohne Known und neuer gültiger Stand. Neu-/Quellfrist unabhängig, keine automatische Dauerschleife.
- `node streaming_page_cache_test.mjs`: tatsächlicher Seitenservice/Cache bewahrt Quellfrist auch nach erneuter erfolgreicher Netzwerkantwort; abgelaufene Einträge werden nicht frisch wiederverwendet.
- `node review49_p07_browser_test.mjs`: tatsächlicher StreamingTab zeigt die Quellenwarnung bei frischem Known unabhängig davon an.
- Grenze: SQL, Controller/Service und UI sind fokussierte lokale Durchstiche, kein gemeinsamer vollständiger App-/Login-End-to-End-Lauf.

### KD-REV-E14-002 · Manueller Refresh

`refreshKatalog` erhöht eine eigene Revision, die zum Kontext des Seitencontrollers gehört. Diese Invalidierung bleibt im inaktiven Tab erhalten, auch bei wertgleichen Diensten, Master- und Fristenbuchdaten.

- `node review49_p07_state_test.mjs`: führt den aktuellen App-Refreshcallback und echten Controller aus. Frischer vollständiger Record wird ohne Refresh wiederverwendet; mit Refresh im inaktiven Tab genau einmal neu gelesen, trotz wiederholter identischer Kontextübergaben. Neue reine Titelkorrektur erscheint; verspätete alte Antwort überschreibt sie nicht.
- `node streaming_page_state_test.mjs`: bestehende Account-, Abort-, Query-, Wiederaufnahme- und Neu-Ablaufgrenzen bestanden.
- Grenze: App-Callback und Controller mit synthetischem Service; kein vollständiger DatenTab-UI-Boot.

### KD-REV-E06-003 · Dauerhafte progressive Neu-Anker

Der additive RPC-Helfer liefert qualifizierte `fensterBeginn`-/`verbrauchtBis`-Werte für die tatsächlich gelieferte Seite; `newSince` bleibt kompatibel. Der Controller akzeptiert nur aktuelle erfolgreiche Seiten. Der Neu-Hook bindet die Anker an Owner, Storagegeneration und Dienstauswahl und persistiert sie im bestehenden Fristenbuch. App wartet auf dessen Hydration vor dem Seitenstart. Ein Rücklauf verursacht höchstens die nötige Kontextneuabfrage, keine Endlosschleife.

- `node review49_p07_pg_test.mjs`: frischer synthetischer PG17-Cluster; aktuelle Base-, Latency- und additive P07-Migration (letztere auch zweimal angewandt). Tatsächlicher RPC, tatsächlicher Controller und tatsächlicher React-Neu-Hook mit lokalem Mock-Gerätespeicher.
- Tag 1 Zugang → Tag 2 Abgang → Tag 4 Wiederzugang: Tag-1-Beginn/Tag-4-Verbrauch werden im reinen Seitenpfad gespeichert. Tag 16 nach tatsächlichem Katalogersatz mit nur Tag-4-Restdiff und React-Remount: `items=[]`, `total=0`, `counts.new=0`. Sensitivitätskontrolle ohne gespeicherten Anker liefert fälschlich 1 und beweist die notwendige Schutzwirkung.
- Ein echter Zugang an Tag 16 startet ein neues Fenster; wiederholte Reads erzeugen weder Verlängerung noch zusätzlichen Speicherwrite. Fremde Auswahl, anderer Account und obsolete Callback-/Epochbelege werden verworfen. Verbundene React-Hooks stabilisieren sich nach genau zwei Seitenreads. Kein Vollkatalogfallback.
- `node streaming_pages_pg_test.mjs`: vorhandene Anker, v2-Übergang, MotN-Zugangszeiten, Pagination, Filter, Identitätskonflikte, Cursor-, ACL- und synthetische Performance-Nachbarn gegen die neue Migration bestanden. `node streaming_pin_neu_test.mjs` prüft die bestehende JS-Neu-Projektion.
- Testuhr: Für den Tag-1/2/4/16-Verlauf wird ausschließlich die Initialisierung von `v_now` in der temporär installierten SQL-Funktion auf einen festen Zeitpunkt gesetzt; Query-/Ankerlogik ist aktueller Produktcode. Danach wird die unveränderte Migration wieder installiert. Keine Produktionsuhr oder Produktdatei wird dafür verändert.
- Grenze: Anker bleiben ein abgeleiteter Gerätecache; ein nicht verfügbarer Gerätespeicher kann nicht dauerhaft speichern. Provider-Pruning-Kadenz und physischer Reload wurden nicht remote geprüft.

### KD-REV-E12-002 · Tastatur-Disclosure

Kino-Kompaktkarten und Streamingkarten haben native, benannte Buttons mit `aria-expanded`. Buttons und Kindaktionen sind Geschwister, keine verschachtelten Controls. Native Enter-/Space-Semantik übernimmt der Browser.

- `node review49_p07_browser_test.mjs`: aktueller Produktcode mit echten CSS-/Fontimports, Chromium **und WebKit**, 44 Szenarien. Kino/Läuft auch, persönliche Kinoempfehlung, Streaming Alles/Neu jeweils ungesehen/gesehen/zugeordnet sowie Legacy. Native Tabfolge, Enter und Space prüfen offenen/sichtbaren Zustand und `aria-expanded` ohne Statuswrite durch Disclosure.
- Links, Pins, Merkliste, Gesehen, Formzugang und Kino-Terminbuttons lösen kein zusätzliches Disclosure aus; programmatischer Suchfokus öffnet weiterhin. Layoutbreiten 320/390/1280 px ohne horizontalen Überlauf, mindestens 44 px Triggerhöhe, keine verschachtelten Controls, keine Page-Errors.
- macOS-WebKit verwendet die plattformübliche native Option-Tab-Eingabe für alle Controls (wie vorhandene Repo-Browsertests); Chromium normales Tab. Externe Kindlink-Ziele werden vom Harness lokal beantwortet. Tatsächliche externe Requests: 0.
- `node kino_mobile_filter_test.mjs`, `node kino_personal_recommendations_test.mjs`, `node streamingtab_filter_panel_default_test.mjs`: angrenzende Filter-, Profil- und Fokusbedingungen bestanden.
- Grenze: echte lokale Browser, synthetische Daten; kein physisches iPhone, kein Screenreader und kein vollständiger Account-End-to-End-Test.

## Ausgeführte Befehle und Ergebnisse

Alle folgenden Befehle liefen in diesem Worktree mit Exit 0. Kein eingefrorener Bug-Harness-PASS wird als Fixnachweis verwendet.

| Befehl | Ergebnis |
| --- | --- |
| `node review49_p07_generation_test.mjs` | 4 Fehler-/Heilungspfade + Same-Generation-Kontrolle |
| `node review49_p07_state_test.mjs` | 5/5 Prüfgruppen |
| `node review49_p07_pg_test.mjs` | 8/8; 4 manuelle Seitenreads, 0 Vollfallbacks; zusätzlich 2-Read-Hook-Rückkopplung |
| `node review49_p07_browser_test.mjs` | 44 Szenarien, Chromium/WebKit, externe Requests 0 |
| `node streaming_pages_pg_test.mjs` | 18/18 |
| `node streaming_page_state_test.mjs` | 22/22 |
| `node streaming_progressive_integration_test.mjs` | 5/5 SQL-Service-Controller-Gruppen; bestehender RPC-Vertrag ohne neue Zusatzfelder bleibt kompatibel |
| `node streaming_page_cache_test.mjs` | 13/13 |
| `node streaming_identity_index_test.mjs` | 6/6 |
| `node streaming_progressive_ui_test.mjs` | PASS: Fehler/Filter/Fokus/Session/Leerzustände |
| `node streaming_pin_neu_test.mjs` | 45/45 |
| `node staffeln_test.mjs` | 8/8 |
| `node staffel_pipeline_test.mjs` | PASS |
| `node katalog_test.mjs` | 116 Checks + 13 Identitätstests |
| `node kino_personal_recommendations_test.mjs` | 5/5 |
| `node kino_mobile_filter_test.mjs` | 15/15 |
| `node streamingtab_filter_panel_default_test.mjs` | 6/6 |
| `git diff --check` | PASS |

Frische PG17-Cluster verwenden `/Applications/Postgres.app/Contents/Versions/17/bin`, isolierte temporäre Datenverzeichnisse und nur Unix-Sockets. Cleanup im `finally`. Erste Sandboxstarts von PG (`shmget`) und Browser (`MachPort`) scheiterten vor Testausführung; nach genehmigter lokaler Eskalation bestanden die Läufe. Keine automatische Approval-Ablehnung. Während des Baus korrigierte Harnessfehler (Fake-Timerverbrauch, Null-Erwartung, Mock-No-op-Write, lokale Popup-MIME) sind keine Produkt-PASS-Nachweise. Positive Identitätsfixtures wurden um erforderliche Typ-/Jahresdaten ergänzt, nicht durch gelockerte Produktassertionen grün gemacht.

## Lieferung und Integrationsnaht

Produktdateien: `src/App.jsx`; `src/controllers/useStreamingNeuController.js`; `src/controllers/useStreamingPageController.js`; `src/lib/katalog.js`; `src/lib/staffeln.js`; `src/lib/streamingPage.js`; `src/styles/streaming-progressive.css`; `src/tabs/KinoTab.jsx`; `src/tabs/StreamingTab.jsx`; `supabase/migrations/20260917120000_review_streaming_freshness_anchors.sql`.

Tests/Fixture: `review49_p07_generation_test.mjs`, `review49_p07_state_test.mjs`, `review49_p07_pg_test.mjs`, `review49_p07_browser_test.mjs`, `review49_p07_browser_fixture.jsx`, `staffeln_test.mjs`, `streaming_identity_index_test.mjs`, `streaming_page_cache_test.mjs`, `streaming_pages_pg_test.mjs`, `streaming_progressive_ui_test.mjs`; dieser Evidenzbericht.

Die additive Migration folgt auf die bestehenden Streaming-Migrationen und erhält `format:1`, Requestvertrag, Cursorbindung und bisherige Berechtigungen; der neue Helfer ist für public/anon/authenticated gesperrt. Alter Client ignoriert zusätzliche Antwortfelder; neuer Client toleriert fehlende Felder, der vollständige progressive Anker-/Quellfristnachweis benötigt die neue Migration. Gemeinsame App-Integrationsstelle: Ankerhydration/Callback und Refreshrevision; keine P09-StartTab-Naht geändert. Neue Testbefehle stehen oben, `package.json` bleibt unverändert.

Nicht durchgeführt: Vollsuite, Build, Push, CI, Deploy, Shared-Migration, Live-/Providerrequest, Remote-Readback oder physische iPhone/PWA-Abnahme. Meister integriert und setzt den übergreifenden Abschlussstatus.

## Restauftrag · UI-Follow-up-Testvertrag

Basis: `ac8fca5fc23b55bb0c583eb86f1dcfeedda2c142`, Branch `codex/review49-p07-rest-20260917`, Worktree `/private/tmp/kd-review49-p07-rest`.

Klassifikation des Master-Gate-Fehlers (`npm-test.log`, `ui_library_followup_test.mjs:87`): **veraltete positive Testfixture und veraltete Shortcut-Erwartung, kein neu bestätigter Produktfehler**. Die bisherige Watchmode-Fixture ließ auf beiden Seiten Pflichtbelege für Jahr und Werkart weg. Eine weitere Assertion akzeptierte allein eine existente `bekannteId`, obwohl der externe Titel weder dazu passende Titel-/Jahr-/Typbelege noch eine gemeinsame starke ID hatte. Beides widerspricht dem aktuellen gemeinsamen Resolver und den Korrekturzielen E06-002/E11-003: Eine historische/voraufgelöste Referenz ersetzt keinen aktuellen Masterabgleich. Keine Produktlockerung und keine Produktdateiänderung.

Der vollständig gelesene Follow-up-Test prüft weiterhin alle bisherigen 22 unabhängigen UI-/Editorverträge. Seine Navigationsgruppe umfasst jetzt 22 Assertions gegen den direkt aus `StreamingTab.jsx` gebündelten aktuellen Export:

- Positive vollständige Watchmode-Identität; Korrektur einer fremden gespeicherten Status-ID; falsche Known-ID überschreibt das richtige Ergebnis nicht.
- Eindeutiger exakter Titel/Jahr/Typ bleibt mit und ohne Known-Vorzuordnung nutzbar.
- Fremde existente Status-ID, bloße Known-ID, gelöschtes Known-Ziel und ungeladener Master bleiben ohne Navigation.
- Die ursprüngliche unvollständige ID-only-Fixture ist als explizite Negativkontrolle erhalten; fehlendes Jahr beziehungsweise fehlender Typ wird einzeln auf externer und Masterseite geprüft.
- Typ-, Jahres- und weitere-ID-Konflikte bleiben trotz existenter Status-/Known-ID gesperrt. Doppelte oder widersprüchliche starke Kandidaten werden in beiden Masterreihenfolgen verworfen; Known kann auch Titelmehrdeutigkeit nicht auflösen.

Fokussierte Nachweise auf dieser Basis, jeweils Exit 0:

| Befehl | Ergebnis |
| --- | --- |
| `node ui_library_followup_test.mjs` | 44/44 Assertions: 22 Navigation, 22 UI-/Editorverträge |
| `node external_title_identity_test.mjs` | 13/13 |
| `node streaming_identity_index_test.mjs` | 6/6 |
| `node staffeln_test.mjs` | 8/8 |
| `node review49_p07_state_test.mjs` | 5/5 Prüfgruppen, einschließlich Konflikt-/Historienerhalt |
| `git diff --check` | PASS |

Write-Fläche ausschließlich `ui_library_followup_test.mjs` und dieser Bericht. Keine Vollsuite, kein Build, keine Browserwiederholung, kein Push/Deploy/Live-/Providerzugriff. Der erneute Gesamtabschlusslauf bleibt beim Meister.

## Zweites Testdelta · Asynchrone Streaming-Persistenz

Basis: `d4aab9acdee08877aa3129225f3c8bada4f02126`, Branch `codex/review49-p07-rest2-20260917`, Worktree `/private/tmp/kd-review49-p07-rest`.

Der vollständig gelesene `async_persistence_ui_test.mjs` erwartete ab Zeile 812 eine Übernahmefrage, obwohl seine Streaming-Fixture ausdrücklich `master: null` übergab. **Klassifikation: veraltete Fixture-Vorbedingung, kein Produktfehler.** E06-002 verlangt die bestätigte aktuelle vollständige Mastergrundlage; der bestehende Guard verhindert daher korrekt die Neuanlage-/Zuordnungsentscheidung bei noch ungeladenem Master. Der Master-Faillog `npm-test-final.log` entspricht genau diesem Pfad. Keine Produktdatei verändert.

Die ursprüngliche asynchrone Persistenzabnahme bleibt erhalten und wird präzisiert: Ein geladener leerer Master (`[]`) erlaubt die Nachfrage. Der Test hält zunächst das `addFilm`-Promise offen und beweist null Statuswrites bis zur bestätigten String-ID. Danach prüft er genau einen ausstehenden Statuswrite mit der richtigen Mediathek-ID und Gesehen-Markierung sowie die gesperrte Speicheraktion. Bestätigtes `false` bewahrt die Nachfrage und zeigt keinen Scheinerfolg. Der Mock behandelt den unveränderten Legacy-Statusabgleich als No-op, statt ihn irrtümlich als Persistenz zu zählen. Die Titelfixture verwendet nun eine gültige numerische Watchmode-ID.

Ein eigener Negativfall mit `master: null` öffnet reale Details im StreamingTab, bestätigt das Fehlen der Erstellen-/Zuordnungsaktion und betätigt „Als gesehen markieren“: keine Übernahmefrage, keine Filmanlage und kein Statuswrite. Die anderen Persistenz-, Editor-, Queue- und Kontogrenzprüfungen der Datei bleiben unverändert.

Fokussierte Befehle auf dieser Basis, jeweils Exit 0:

- `node async_persistence_ui_test.mjs`: **72/72**, einschließlich der beiden ursprünglichen Streaming-Abnahmen und vier ergänzten Checks.
- `node ui_library_followup_test.mjs`: **44/44**.
- `node review49_p07_state_test.mjs`: **5/5** Prüfgruppen.
- `node streaming_identity_index_test.mjs`: **6/6**.
- `node staffeln_test.mjs`: **8/8**.
- `git diff --check`: PASS.

Write-Fläche ausschließlich `async_persistence_ui_test.mjs` und dieser Bericht. Keine Produktlockerung, Vollsuite, Build, Browserwiederholung, Remote-/Providerwirkung oder neue Integrationsnaht. Den Gesamtabschlusslauf führt der Meister fort.

## Staging-Testdelta · Portable PostgreSQL-Binaries

Basis `8ae8c6e066bd5aeaf2e7746cef904dfb7797a0ae`; separater Branch `codex/review49-p07-pg-portability-20260917`, Worktree `/private/tmp/kd-review49-p07-pg-portability`. Der absolute macOS-Pfad war ein Test-Infrastrukturfehler auf Ubuntu. `review49_p07_pg_test.mjs` verwendet nun das bestehende Repo-Muster: `KD_TEST_PG_BIN` → Postgres.app → erfolgreiches `pg_config --bindir` → `/usr/lib/postgresql/17/bin`. Ein Kandidat muss `initdb`, `pg_ctl`, `psql` und `postgres` enthalten. Fehlende Infrastruktur bleibt eine harte Assertion; kein Skip. `tmpdir()` war bereits portabel. Produkt, SQL, Fixtures, Package und Workflow sind unverändert; sämtliche fachlichen Assertions bleiben erhalten.

- `KD_TEST_PG_BIN=/Applications/Postgres.app/Contents/Versions/17/bin node review49_p07_pg_test.mjs`: Exit 0, **8/8**, vier Seitenreads, null Vollfallbacks; frischer ausschließlich lokaler Socketcluster, anschließend entfernt.
- Alle vier Binary-Aufrufe mit `--version`: Exit 0, jeweils **PostgreSQL 17.10 (Postgres.app)**.
- `node --input-type=module` mit `vm.runInNewContext`: Der unverändert aus der Testdatei extrahierte Abschnitt `const configured=` bis unmittelbar vor `const folder=` wurde mit injizierten `spawnSync`-/`existsSync`-Ergebnissen geprüft, **6/6**: gültiger Override gewinnt; macOS-Fallback gewinnt; ohne macOS gewinnt der konfigurierte Pfad; ohne `pg_config` gewinnt der Linuxpfad; Client-only-Override fällt auf vollständiges Linuxverzeichnis zurück; ausschließlich Client-/fehlende Serverbinaries werfen `PostgreSQL server binaries are required`.
- `git diff --check`: PASS.

Write-Fläche nur der PG-Test und dieser Deltaabschnitt. Keine Vollsuite, keine Remote-/Providerwirkung. Die Pfadlogik für Ubuntu ist lokal synthetisch belegt; der echte SQL-/React-Solltest lief auf macOS mit PG17. Ein tatsächlicher Ubuntu-CI-Lauf ist damit noch nicht behauptet.
