# P06 / M4 – Filmwissen-Identität und KI-Fehler

Basis: `84cde78b18bc2ba060f348613ba4304668901ffc`.
Branch: `codex/review49-p06-filmwissen-20260917`.
Stand: 2026-09-17. Alle vier Originaltickets vollständig gelesen und am heutigen
Produktcode bestätigt. Keine Originaltickets oder Gesamtregister geändert.
Status: **GEBAUT / lokal fokussiert GETESTET**. Commit siehe Paketübergabe.

## E10-004 – typisierte TMDB-Identität

Der vorhandene Zwei-Felder-Vertrag bleibt erhalten: Namespace `tmdb`, Kennung
`movie:348` beziehungsweise `tv:348` (Filmreihen: `collection:`). Helper,
Transport, Read-/Recherche-Deduplizierung, Forecast-Builder, Handler,
Read-/Synthese-RPC und Adapter behalten den Typ. Der Client verwirft Cachewerke
mit abweichendem Typ als Cache-Miss; der Forecast übernimmt sie nicht als
Filmwissen-Provenienz. Die persönliche Prognose funktioniert dann weiterhin
mit persönlicher WARUM-Schätzung. Der reine Filmadapter lehnt untypisierte,
TV- und Collection-TMDB-Kennungen vor Quellenzugriff ab. Ein fehlender
Serienbericht endet im Client als derzeit gesperrter Quellenweg; gültige
vorhandene Serien-IMDb-Belege bleiben lesbar. Reguläre Filme bleiben
recherchierbar. Die FlixPatrol-Naht übersetzt ausschließlich im Handler zurück
zur numerischen ID plus bereits geprüftem separatem Werktyp; der gemeinsame
FlixPatrol-Vertrag bleibt unverändert.

Die additive Migration `20260917110000_review_filmwissen_identity.sql`
übernimmt alte Film-TMDB-Zuordnungen als `movie:` mit ihrem bisherigen
Prüfstatus. Nicht-Film-Altzuordnungen werden mit dem passenden Präfix **gesperrt**
und verlieren ihren Prüfzeitpunkt; sie bedürfen einer ausdrücklichen erneuten
Prüfung einer typisierten Kennung. Keine alte numerische Kennung wird weiter
akzeptiert. CHECK, Beziehungstrigger und Read-/Synthese-Typvergleich verhindern
auch DB-seitige Typwidersprüche. Bestehende RPC-/Tabellenrechte bleiben erhalten.
Historische Migrationen bleiben unverändert.

Belege: 7 neue direkte Modul-/Service-/Transport-/Adaptertests, ein neuer
Forecast-Auftragstest, neue vollständige Handlerfälle für Serienforecast,
Film-TMDB-Synthese und providerfreien Serien-Stopp sowie die bestehenden
IMDb-/Film-Kontrollen. Echte PostgreSQL-17-Probe: **19/19** mit frischem
synthetischem Cluster, echter Basismigration, echter Synthese-Vorgängermigration,
echtem Reaperfunktionskörper und unverändert eingespielter neuer Migration.
Film und Serie derselben Nummer erhalten separate belegte Reads und
`cache_hit` über die echte Synthese-RPC unter `service_role`. Geprüft sind auch
Altkennungen, Typwidersprüche, fehlende/inaktive Auth und RPC-/Tabellen-ACLs.
Die lokalen `auth.uid`-/Accountaktivitätsfunktionen und die Limits-Tabelle sind
explizite Plattform-Doubles; dies ist kein vollständiger Supabase-Betrieb.

## E10-003 – Quellenfrist umfasst den Body

Der gemeinsame `holeJson`-Timer bleibt über HTTP-Prüfung und begrenztes
Bodylesen aktiv. Abbrüche nach den Headern werden ebenfalls `adapter-timeout`;
Reader werden abgebrochen/freigegeben und Timer auch auf Fehlerpfaden entfernt.
Bestehende fachliche Quellenfehler behalten ihre Klassen.

Belege: **32/32** neue Tests beider öffentlicher Adapter mit echten
ReadableStreams und lokalen Fetch-Doubles. Je Adapter Headerstall, sofortiger
Body-Stall, Stall nach kleinem Chunk, rechtzeitiger JSON-Body samt gelöschtem
Timer; Redirect, 429/503, HTTP, Content-Type, deklarierte und gelesene
Übergröße, UTF-8, JSON, fehlender Body, Netz-/Bodylesefehler. Bei 25-ms-Fristen
enden Stalls lokal ungefähr nach 25–29 ms; Testtoleranz 500 ms. Bestehende
vollständige gültige Wikidata-/LOC-Fälle und Quellenregeln: **17/17** grün.

## E10-001 – JSON-Wurzelform

Direkt nach Parse werden Null, Array und primitive Wurzeln kontrolliert als
400 / `invalid-response` / `json-wurzel-kein-objekt` abgewiesen. Auth, DB,
Aktivierungsgate und Provider werden dafür nicht aufgerufen.

Belege: vollständiger Handler, sieben Wurzelvarianten unter aktivem und
inaktivem KI-Schalter, jeweils ohne jeden Transportaufruf. Gültige Objekte mit
Null-Payload behalten ihre Gates. Bestehende OPTIONS-, Nicht-POST- und
Malformed-JSON-Kontrollen sind im fokussierten Handlerlauf enthalten.

## E10-002 – ehrlicher Nullkosten-Diagnoseabschluss

Fetch-/Bodyfehler werden unabhängig von bereits empfangenen Headern behandelt.
Bodyabort liefert `anbieter-zeitgrenze`; unlesbare oder ungültige Katalogdaten
liefern `anbieter-antwort-ungueltig`. Jeder Fehler schließt genau einmal mit
`fehler`, seiner sicheren Fehlerklasse und Kosten/Tokens 0. Erfolg setzt ein
vollständig dekodiertes `data`-Array mit gültigen Modellobjekten voraus.
Strukturell gültige leere Listen bleiben gültig; P8 sperrt sie weiterhin.

Belege: Bodyabort, Bodylesefehler, kaputtes JSON, Null, Arraywurzel, fehlende
Daten, falscher Datentyp, ungültiger Listeneintrag. Bestehende gültige Liste,
Fetch-/HTTP-Fehler, Startablehnung, Not-Aus und Providerfreigaben grün. Der
unveränderte tatsächliche P8-Quellblock wird für eine leere Liste lokal mit
Doubles ausgeführt und stoppt vor den zahlenden Proben. Kein Smoke gestartet.

## Reproduzierbare fokussierte Befehle

Alle Befehle aus dem Paketcheckout, ohne Installation oder Netzfreigabe:

```sh
node --test review49_p06_identity_test.mjs review49_p06_transport_test.mjs
# 39/39, Exit 0
node review49_p06_sql_test.mjs
# 19/19, Exit 0; PG17_BIN optional, Default /Applications/Postgres.app/Contents/Versions/17/bin
node_modules/.bin/deno test --allow-env --allow-read --node-modules-dir=manual --no-lock --cached-only --filter '/^(P06|H[456]|FW|FF3|C8|C11|C12)/' ai_task_test.ts
# 41/41, 308 gefiltert, Exit 0; Typecheck eingeschlossen, keine Netzwerkberechtigung
node filmwissen_service_test.mjs
# 21/21, Exit 0
node filmwissen_quellen_adapter_test.mjs
# 17/17, Exit 0
node filmwissen_contract_test.mjs
# 14/14, Exit 0
node prognose_auftrag_test.mjs
# 21/21, Exit 0
node vorbewertung_service_test.mjs
# 13/13, Exit 0
git diff --check
# Exit 0
```

Der erste PG-Start scheiterte an der Sandbox-Shared-Memory-Grenze vor dem
SQL-Lauf. Der autorisierte lokale Eskalationslauf fand einen PL/pgSQL-Syntaxfehler
in einer CASE-Bedingung, der korrigiert wurde. Der finale frische Clusterlauf
ist grün und wird vollständig beendet/entfernt. Ein erster Deno-Typecheck fand
nur einen Testfixture-Typfehler; korrigiert, finaler Typecheck grün.

## Integrationsnaht und Liefergrenzen

Client, `ai-task` und Migration benötigen eine gemeinsame Lieferung: alte
numerische TMDB-Anfragen scheitern absichtlich geschlossen; vor der Migration
versteht die DB die neuen typisierten Schlüssel nicht. Kein stiller Fallback
auf numerische Schlüssel. Die produktive Bestandsmenge der zu sperrenden
Nicht-Film-Altkennungen wurde nicht abgefragt. Ein Serienquellenadapter ist
weiterhin nicht implementiert.

Keine fremde Write-Fläche, Dependencies, App.jsx, Shared-Helper oder Auth-/Storage-
Implementierung geändert. Keine globale Suite, kein Build, Push, Deploy,
Remote-/Shared-Write, echter Providerrequest oder kostenpflichtiger Test.
CI, Deployment/Readback und physische iPhone/PWA-Abnahme sind **NICHT BELEGT**.

## Delta-RESTAUFTRAG E10-004 – persönliche TMDB-ID an der Formulargrenze

Basis: `aeca490f2317a7a3276ed8d3a3d445361af66c1c`, isolierter Worktree
`/private/tmp/kd-review49-p06-rest`, Branch
`codex/review49-p06-rest-20260917`.

Die unabhängige Abschlussprüfung fand eine durch P06 verursachte Regression:
`FilmForm` übergab seine numerische persönliche `tmdb_id` an den jetzt
absichtlich typisierten Filmwissen-Normalisierer. Dadurch wurde eine gültige,
nicht bearbeitbare Streaming-/Finder-ID beim Bauen des Save-/Forecast-Eintrags
weggelassen. Der Prüfer-Repro
`/private/tmp/kd-review49-verification-evidence/filmform-tmdb-regression.mjs`
wurde vollständig gelesen; die Ursache war auf der Restauftragsbasis direkt
im tatsächlichen Formularcode vorhanden.

Der kleine Formularfix verwendet ausschließlich für TMDB den vorhandenen
Normalisierer persönlicher externer Titelkennungen. Der bisherige Formularzaun
von 1–18 Ziffern bleibt ausdrücklich vorgeschaltet: kein Präfix, kein negativer
Wert, kein Dezimalwert, kein Freitext und kein zu langer Wert. Führende Nullen
werden normalisiert; der gespeicherte Wert ist weiterhin ein numerischer
**String**, kein typisierter Filmwissenschlüssel. IMDb/Wikidata verwenden ihren
unveränderten strikten Normalisierer. `movie:`/`tv:` wird weiterhin ausschließlich
an der Filmwissen-/Forecast-Grenze ergänzt. Keine Lockerung der Film-/Serientrennung,
keine Änderung der P04-Typ-/Entwurfs-/Prognosekorrekturen.

Alle direkten Produkt-/Werkzeugverwendungen von `normalisiereFilmkennung`
wurden gezielt geprüft (`rg` in `src` und `tools`). Zwei weitere betroffene
Grenzen waren `tools/filmwissen_live_target.mjs` und die reine
`liesFilmwissenKennung`-Funktion in `tools/ai_smoke.mjs`: Ihre Eingaberegex
ließen kein typisiertes TMDB-Ziel durch. Nach ausdrücklicher Scope-Erweiterung
durch den Meister akzeptieren beide nun ausschließlich explizite
`tmdb:movie:<nummer>`-Ziele. Numerische Altziele, `tv:` und `collection:` bleiben
abgewiesen; IMDb/Wikidata, Defaultwahl, Dublettenstopp, Budget-, Lock-, Gate- und
Requestgrenzen bleiben unverändert. Kein Smoke-Einstieg wurde ausgeführt.

### Tatsächliche Regression und lokale Belege

`review49_p06_form_identity_test.mjs` bündelt und mountet den echten `FilmForm`
mit JSDOM. Die echte Save-Rückgabe wird am `onAdd`-Persistenzrand über JSON und
lokalen Browserstorage geschrieben/zurückgelesen. Der Prognoseknopf durchläuft
den echten `erstelleVorbewertungsErgebnis`-Service samt Payload-Builder bis zu
einem lokalen `ai.runTask`-Double, das den Auftrag aufzeichnet und kontrolliert
stoppt. Es wird keine künstliche Prognose gespeichert.

36 Szenarien prüfen Film und Serie: nicht bearbeitbare IDs als String, Zahl
und mit führenden Nullen; manuelle editierbare IDs mit Leerraum/Nullen;
Null-ID, Minus, Buchstaben, Dezimalwert, falsche Präfixe und Überlänge.
Gültige Saves enthalten `tmdb_id: "77"`, persönliche Forecast-IDs bleiben
`tmdb: "77"`, Filmwissen/Forecast-Filmkennung lautet `movie:77` oder `tv:77`.
Manuell ungültige IDs blockieren Save/Forecast; nicht bearbeitbare ungültige
Quell-IDs werden wie bisher ausgelassen und nie als starke Kennung übertragen.
Alle 36 Szenarien sind grün, **0 Netzwerkaufrufe**.

Die Werkzeugregression prüft beide tatsächlichen Parser. Beim Smoke wird nur
der abgegrenzte reine Funktionskörper lokal mit injizierten Werten ausgeführt;
kein Import des Smoke-Einstiegs, keine CLI und kein Providerpfad. Ein erster
neuer Test erwartete irrtümlich automatische Dublettenentfernung; der bestehende
Dublettenstopp wurde erhalten und die Sollassertion auf dessen Ablehnung
korrigiert. Kein Produktguard wurde dafür verändert.

Alle folgenden Befehle im Restauftrags-Worktree: **Exit 0**.

| Befehl | Ergebnis |
| --- | --- |
| `node review49_p06_form_identity_test.mjs` | 36/36 echte Formular-/Save-/Forecast-Szenarien, 0 Netz |
| `node --test review49_p06_identity_test.mjs` | 7/7, typisierte Film-/Serientrennung erhalten |
| `node filmwissen_live_target_test.mjs` | 9/9, beide Werkzeugparser lokal geprüft |
| `node budget_agent_test.mjs` | 56/56, 0 echte KI-Aufrufe |
| `node rating_followup_ui_test.mjs` | 22/22 |
| `node review49_p04_personal_test.mjs` | 112 Sollprüfungen, 0 Netzwerk-/Provideraufrufe |
| `node prognose_auftrag_test.mjs` | 21/21 |
| `node vorbewertung_service_test.mjs` | 13/13 |
| `node etappe8_produktintegration_test.mjs` | 11/11 |
| `git diff --check` | sauber |

Delta-Schreibfläche: `src/components/EintragForm.jsx`,
`tools/filmwissen_live_target.mjs`, `tools/ai_smoke.mjs`,
`filmwissen_live_target_test.mjs`, neuer `review49_p06_form_identity_test.mjs`
und dieser Evidenznachtrag. Helper, Handler, Migration, P04-Nachbarn und
Shared-Helper bleiben unverändert. Der neue Integrationsbefehl ist
`node review49_p06_form_identity_test.mjs`.

Grenzen: JSDOM/JSON-Save-Rand ist keine echte Account-/Remote-Persistenz oder
physische Browserabnahme. Keine globale Suite, kein Build, Push, Deploy,
Live-/Providerlauf, Budgetmessung oder Shared-Write. Die vorherige gemeinsame
Liefernaht von Client, Function und Migration bleibt bestehen; dieses Delta
fügt keine neue Migration oder Anbieteroperation hinzu.

## Test-only Delta – portable PostgreSQL-Erkennung für Staging-CI

Basis `8ae8c6e066bd5aeaf2e7746cef904dfb7797a0ae`; isolierter Branch
`codex/review49-p06-pg-portable-20260917`, Worktree
`/private/tmp/kd-review49-p06-pg-portable`. Ausschließlich der SQL-Test und dieser
Nachtrag wurden geändert. Produkt, SQL, Assertions, Workflow und npm-Scripts
bleiben unverändert.

`review49_p06_sql_test.mjs` folgt nun dem vorhandenen Muster aus
`data_plan_foundation_pg17_test.mjs`: Auswahl vollständiger Server-Bin-Verzeichnisse
(`initdb`, `pg_ctl`, `postgres`, `psql`) in der Reihenfolge `KD_TEST_PG_BIN`,
kompatibles bisheriges `PG17_BIN`, erfolgreiches `pg_config --bindir`,
Postgres.app 17, Linux `/usr/lib/postgresql/{17,16}/bin`. Der gewählte Pfad wird
sichtbar ausgegeben. Fehlen vollständige Binaries, endet der Test vor der
Clusteranlage mit Fehler; es gibt keinen Skip. `tmpdir()` war bereits portabel.
Die Ergebniszeile behauptet bei einer anderen installierten Version nicht mehr
pauschal PostgreSQL 17.

Prüfungen, jeweils Exit 0:

- `node --check review49_p06_sql_test.mjs` und `git diff --check`.
- `node /private/tmp/p06-pg-path-choice.mjs`: **9/9**. Der tatsächliche Auswahlblock
  des aktuellen Testquelltexts wird in einer VM mit injizierten `pg_config`-/
  Dateisystemantworten ausgeführt: KD-Vorrang, Legacy-PG17_BIN, Linux-pg_config,
  macOS-Fallback, Linux-17-Fallback, Linux-16-Fallback, Client-only-pg_config,
  komplett fehlende Infrastruktur und fehlendes `postgres`-Binary. Die letzten
  zwei Fälle verlangen ausdrücklich einen Fehler; kein Serverstart.
- `node review49_p06_sql_test.mjs`: **19/19** unveränderte SQL-Sollprüfungen auf
  echtem PostgreSQL **17.10**, automatisch über den macOS-Fallback gefunden.
  Frischer synthetischer Cluster, nur Unix-Socket, vollständig beendet/entfernt.
  Der erste Sandboxstart scheiterte vor SQL an Shared-Memory-Rechten; der
  autorisierte lokale Eskalationslauf bestand.

Linux-Pfadwahl ist lokal simuliert belegt, eine tatsächliche Ubuntu-Ausführung
ist noch nicht belegt. Keine Vollsuite, kein Build, Push, Deploy oder
Remote-/Providerwrite. Die lokale Auswahlprüfung ersetzt nicht den CI-Lauf.
