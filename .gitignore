# Build-Ausgaben
node_modules
dist
dist-single
dist-ssr
test-results/
playwright-report/
*.local

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.sw?

# lokale Daten & Schlüssel — NIE versionieren
.env
.env.*
!.env.example
streaming-daten/
kino-daten/
Archiv/

# Transfer-/Aufräum-Reste (nicht committen)
_to_delete/
_kd_tree.tgz
# Claudes rohe Audit-Übergabe bleibt lokal bis zum Abschluss; die verifizierte
# Fortsetzung und Planungsarchäologie liegen versioniert unter docs/.
_audit_handover/

# Lose Debug-/Screenshot-PNGs im Root (echte Icons liegen in public/, Logo in logo/)
/*.png
dist-single-beta/

# Supabase-CLI-Zustand (pro Gerät, gehört nicht ins Repo)
supabase/.temp/

*.bundle

# Roh-Antworten des Eval-Laufs (tools/ai_eval_etappe6.mjs --holen). Enthalten
# nur Suchsätze und Modellausgaben, keine Schlüssel — gehören trotzdem nicht ins
# Repo: sie sind ein Messprotokoll eines Laufs, kein Quelltext.
eval_rohdaten_*.json
